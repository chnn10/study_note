package cn.chnn10.testmyself;

public class MySQL {
    /**
     * ## 面试题
     *
     * 优化
     *   优化数据库的方法
     *   怎么优化MySQL
     *   优化关联查询
     *   优化子查询
     *   SQL语句优化的方法
     *   谈谈MySQL的Explain
     *   优化长难的查询语句
     *   一条SQL执行时间过长，怎么处理
     *   一张表数据有千万，怎么优化
     *   优化UNION查询
     *   项目中sql耗时，统计满查询，慢查询怎么优化
     *   大表的优化方案
     *   使用索引一定能提高查询的性能吗
     *   索引在什么情况下会失效
     *
     * 索引
     *   索引有哪几种类型
     *   创建索引的三种方式
     *   什么情况设置了索引但是无法使用
     *   索引的目的
     *   索引的缺点
     *   建立索引的原则
     *   什么情况不建议使用索引
     *   什么是最左前缀原则，什么是最左匹配原则
     *   怎么知道SQL使用了索引
     *   什么时候使用聚族索引，什么时候使用非聚族索引
     *   索引的分类
     *   怎么看到表格定义的所有索引
     *   主键索引和唯一索引的区别
     *   覆盖索引和回表知道吗
     *   可以使用多少列创建索引
     *   InnoDB引擎的索引策略
     *   联合索引是什么？为什么需要注意联合索引中的顺序
     *   怎么删除索引
     *
     * 事务
     *   事务如何通过日志实现的
     *   什么是幻读、脏读、不可重复读
     *   事务的四大特性
     *   事务的隔离级别
     *   InnoDB事务实现原理
     *   事务的回滚机制
     *
     * 试图
     *   试图有什么用，哪些场景使用
     *   试图的优点
     *   什么是基本表
     *   数据表损坏了怎么修复
     *   
     * 存储过程
     *   什么是存储过程，用来做什么的
     *
     * 表连接
     *   谈谈六种连接和使用场景
     *   内连接、左连接、、右链接的区别
     *   内连接、外连接、交叉连接和笛卡尔积
     *   Join连接有了解吗
     * 主外键
     *   自增主键用完了怎么办
     *   为什么要尽量设定一个主键
     *   主键、外键分别是什么
     *   主键使用UUID还是自增ID
     *   怎么选择合适的分布式主键方案
     *   主键、外键和索引的区别
     *   非主键索引一定会查询很多次吗
     *   数据库自增主键可能会遇到什么问题
     *
     * 数据结构
     *   使用B树的好处
     *   B+树在满足聚族索引和覆盖索引的时候不需要回表查询数据
     *   为什么使用B+，和Hash有什么区别
     *   为什么使用B+，和二叉树的区别
     *   B和B+ 的区别，为什么不使用B
     *   B+树的页子节点都可以存放在哪些东西
     * 锁
     *   MySQL中有哪些锁
     *   使用乐观锁
     *   MySQL遇到死锁怎么办解决
     *   隔离级别与锁的关系
     *   乐观锁和悲观锁怎么实现
     *   锁优化的策略
     *   一条SQL加锁分析
     *   锁类别来分，MySQL有几种锁
     *   锁粒度来分，MySQL有几种
     *   InnoDB锁算法
     *   InnoDB引擎中的行锁怎么实现的
     *   幻读如何解决？
     *
     * 引擎
     *   InnoDB的B+树存储整行数据和主键的值的区别
     *   MySQL中InnoDB引擎的行锁是怎么实现的
     *   MyISAM和InnoDB的区别
     *   InnoDB什么时候会退化为表级锁
     *   MySQL引擎有哪些？各有什么区别、优势，并且底层是怎么实现的；
     *   nnoDB的存储原理，就是它如何维护每个页的
     *
     * 约束
     *   完整性约束包括哪些
     *
     * 日志
     *   binlog有几种录入方式，分别有什么区别
     *   binlog作用？
     
     * MySQL知识
     *   数据库的三大范式
     *   MySQL怎么使用DISTINCT
     *   一条SQL的执行顺序
     *   怎么优化DISTINCT
     *   什么是触发器，使用场景是哪些
     *   如何显示前50行
     *   读写分离常见方案
     *   NULL是什么
     *   优化器的执行过程
     *   like中%和_是什么意思
     *   主从同步延迟的解决方法
     *   MCVV的底层原理
     *   MySQL查询权限的表有几个
     *   字段为什么要去定义为not null
     *   SQL注入漏洞产生的原因，怎么防止
     *   列设置auto increment时候，如果在表中达到最大值，会发生什么
     *   一张表有一列TIMESTAMP，将会发生什么
     *   Select for update的含义
     *   Varchar(50)的含义
     *   Int(20)char(20)和varchar（20）的区别
     *   MySQL数据库服务器性能分析的方法命令有哪些
     *   列为NULL时候，查询是否会使用索引
     *   MySQL记录货币用什么字段
     *   NOE()和CURRENT_DATE()的区别
     *   CHAR()和VARCHAR()的区别
     *   SQL的生命周期
     *   MySQL中的in和exist
     *   存储日期格式，如何考虑时区转换的问题
     *   char和varchar的区别
     *   Count(1)、count(*)和count(列名的区别)
     *   数据库CPU飙升到500%怎么处理
     *   MySQL有哪些数据类型
     *   MySQL中TIMESTAMP和DATESTAMP的区别
     *   MySQL的复制原理和流程
     *   MySQL的基础架构图
     *   SQL语句分为几种
     *   数据库经常使用的函数
     *   MySQL驱动程序是什么
     *   优化LIMIT分页
     *   超大分页怎么处理
     *   MySQL怎么获取当前日期
     *   列的字符串可以是什么
     *   存储用户的密码hash，应该使用什么字段进行存储
     *   分库分表怎么设计
     *   怎么监控你们的数据库，慢日志都是怎么查询的
     *   日志的存储形式
     *   Blob和Text的区别
     *   备份计划
     *   百万级别的数据怎么删除
     *   delete、drop和truncate的区别
     *   MySQL有哪些触发器
     *   主从一致性的校验，怎么做
     *   UNION和UNION ALL
     *   Group by的实现原理是什么
     *   数据库读写分离是怎么实现的？
     *   说说读写分离对于业务有什么好处？
     *   分库分表的设计思路
     *   你们改代码一般是怎么样的？你们会修改数据库，比如说加一个索引吗？
     *   主从同步怎么做到，半同步说一下
     */
}

	• 地址、section、vstart
		○ 地址：代办
		○ Section：代办
		○ Vstart：代办
	• CPU实模式
		○ CPU工作原理 ---> CPU工作原理总结
			§ CPU工作部件主要是：控制单元、运算单元、存储单元
				□ 控制单元：控制单元是CPU的控制中心，CPU经过它的帮忙才知道知道自己下一步要做什么。
				□ 运算单元：运算单元最好理解了，就是进行逻辑运算的。
				□ 存储单元：存储单元主要包括缓存和寄存器，主要存放运算时候需要用到的数据。
			§ CPU工作原理总结
				□ 1：CPU要获取下一条指令，通过程序计数器获取的，主要是读取cs:ip的值可以获取指令的地址。
				□ 2：跟进地址，通过地址总线去内存获取指令，将此指令放在指令寄存器中。
				□ 3：译码器对指令进行解码，知道这条指令的操作码是什么，需要操作的数据是什么，那访问哪个寄存器、内存等信息。
				□ 4：解码之后，就可以开始执行了。
				□ 5：然后修改ip的值，这样就可以一直取指令 --> 执行指令。
		○ 实模式的寄存器
			§ 通用寄存器的介绍
				□ ax累加寄存器：使用频率最高的寄存器。
				□ bx基址寄存器：常用来存储内存地址，用此作用内存的基地址来遍历一片内存区域。
				□ cx计数寄存器：常用于循环指令中的循环次数。
				□ dx数据寄存器：用于存储数据，常常存储的是端口号。
				□ si源变址寄存器：
				□ di目的变址寄存器：
				□ sp栈指针寄存器：
				□ bp基址寄存器：
